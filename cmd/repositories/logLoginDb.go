package repositories

// func CreateLogLogin(logLogin models.LogLogin) (models.LogLogin, error) {
// db := storage.GetDB()

// sqlStatement := `INSERT INTO log_login (user_id, login_at) VALUES ($1, $2) RETURNING id`

// err := db.QueryRow(sqlStatement, logLogin.UserId, logLogin.LoginAt).Scan(&logLogin.Id)
// }
